# Principal Component Analysis vs Linear Discriminant Analysis

Compares unsupervised (PCA) and supervised (LDA) dimensionality reduction on the UCI Wine dataset, projecting both onto 2 components and visualising how well each preserves the three wine-class groupings. LDA, which uses the class labels, separates the classes more cleanly than PCA.
